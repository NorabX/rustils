// extern crate rand;
// extern crate regex;
//
// use rand::{ thread_rng, Rng };
// use rand::distributions::{ Distribution };
// use rand::distributions::uniform::{ SampleUniform };
//
// use regex::Regex;

fn main(){
    // let mut rng = thread_rng();
    // println!("{:?}", rng.gen::<u8>());
    //
    // let re = Regex::new(r"\d{2}");
    // println!("{:?}", re.is_match("2014-01-01"))

    // match re {
    //     Ok(r) => {
    //         for mat in r.find_iter("2014-01-01") {
    //             println!("{:?}", mat.start());
    //         }
    //     },
    //     Err(err) => println!("{:?}", err)
    // }
}
